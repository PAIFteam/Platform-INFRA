apiVersion: v1
kind: Service
metadata:
  name: catalog-api
  namespace: paif
spec:
  type: ClusterIP
  selector:
    app: catalog-api
  ports:
    - name: http
      port: 5004
      targetPort: 8080
      protocol: TCP

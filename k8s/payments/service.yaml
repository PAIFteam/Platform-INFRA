apiVersion: v1
kind: Service
metadata:
  name: payments-api
  namespace: paif
spec:
  type: ClusterIP
  selector:
    app: payments-api
  ports:
    - name: http
      port: 5002
      targetPort: 8080
      protocol: TCP

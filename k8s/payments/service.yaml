apiVersion: v1
kind: Service
metadata:
  name: payments-api
  namespace: paifgames-ns
spec:
  type: ClusterIP
  selector:
    io.kompose.service: payments-api
  ports:
    - name: http
      port: 5002
      targetPort: 8080
      protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: users-api
  namespace: paif
spec:
  type: ClusterIP
  selector:
    app: users-api
  ports:
    - name: http
      port: 5001
      targetPort: 8080
      protocol: TCP

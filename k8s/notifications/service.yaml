apiVersion: v1
kind: Service
metadata:
  name: notifications-api
  namespace: paifgames-ns
spec:
  type: ClusterIP
  selector:
    io.kompose.service: notifications-api
  ports:
    - name: http
      port: 5003
      targetPort: 8080
      protocol: TCP

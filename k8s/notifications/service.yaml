apiVersion: v1
kind: Service
metadata:
  name: notifications-api
  namespace: paif
spec:
  type: ClusterIP
  selector:
    app: notifications-api
  ports:
    - name: http
      port: 5003
      targetPort: 8080
      protocol: TCP
